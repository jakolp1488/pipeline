{
  "name": "pipeline-tiktok-oauth",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: TikTok OAuth backend for Pipeline",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
